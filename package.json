{
  "name": "@jknauber/telltale",
  "version": "0.0.2",
  "type": "module",
  "description": "Local memory loop for Claude Code: stages and promotes durable preferences across sessions.",
  "bin": {
    "telltale": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm --prefix ui run build && rolldown -c && chmod +x dist/cli.js",
    "build:cli": "rolldown -c && chmod +x dist/cli.js",
    "build:ui": "npm --prefix ui run build",
    "dev": "tsx src/cli.ts",
    "dev:link": "npm run build && npm link",
    "dev:unlink": "npm unlink -g @jknauber/telltale",
    "telltale": "tsx src/cli.ts",
    "setup": "tsx src/cli.ts setup",
    "promote": "tsx src/cli.ts promote",
    "typecheck": "tsc --noEmit",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "npm run build && changeset publish",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^22.0.0",
    "rolldown": "^1.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "citty": "^0.2.2",
    "hono": "^4.6.13"
  }
}
